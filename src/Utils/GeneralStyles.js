import { StyleSheet } from 'react-native';

const Style = StyleSheet.create({
    container: {
        flex: 1,
    },
    containerRow: {
        flex: 1,
        flexDirection: 'row',
    },
    centerContentHorVert: {
        justifyContent: 'center',
        alignItems: 'center'
    }
});

export default Style;
